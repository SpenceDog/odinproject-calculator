function Operate(){ // This is going to do our calculations for us.
    alert("a");
}

function writeNum(){ // adds the specified field to the text box.
    var text = document.getElementById('lower-number');
    text.value += ' after clicking';
}